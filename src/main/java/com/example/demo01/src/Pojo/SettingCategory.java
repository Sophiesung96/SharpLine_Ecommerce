package com.example.demo01.src.Pojo;

public enum SettingCategory {
    GENERAL,MAILSERVER,CURRENCY,PAYMENT,MAILTEMPLATE


    // I need to call name() this function to get the enum constant as a string
    // e.g. SettingCategory.GENERAL.name();

}
